import { h } from 'vue';
import { PwaPopup } from './PwaPopup';
const locales = __PWA_POPUP_LOCALES__;
export const PwaPopupWrapper = () => {
    if (__VUEPRESS_SSR__)
        return null;
    return h(PwaPopup, {
        locales,
    });
};
PwaPopupWrapper.displayName = 'PwaPopupWrapper';
export default PwaPopupWrapper;

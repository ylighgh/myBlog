"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePWAPopupOptions = void 0;
/**
 * Resolve options for @vuepress/plugin-pwa-popup
 */
const resolvePWAPopupOptions = (themePlugins, localeOptions) => {
    var _a, _b;
    if ((themePlugins === null || themePlugins === void 0 ? void 0 : themePlugins.pwa) === false || !localeOptions.locales) {
        return false;
    }
    const option = Object.assign({
        locales: {}
    });
    for (const local in localeOptions.locales) {
        const message = ((_a = localeOptions.locales[local]) === null || _a === void 0 ? void 0 : _a.pwaPopupMessage) ||
            "New content is available.";
        const buttonText = ((_b = localeOptions.locales[local]) === null || _b === void 0 ? void 0 : _b.pwaPopupButtonText) || "Refresh";
        option.locales[local] = {};
        option.locales[local].message = message;
        option.locales[local].buttonText = buttonText;
    }
    return option;
};
exports.resolvePWAPopupOptions = resolvePWAPopupOptions;
